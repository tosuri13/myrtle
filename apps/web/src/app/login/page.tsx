import { LoginForm } from "@/features/Auth/components/LoginForm";

export default function LoginPage() {
  return (
    <div className="mx-[24px] flex w-full flex-col items-center justify-center">
      <LoginForm />
    </div>
  );
}
